# Secrets. This line is the single most important one in the file.
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Django
db.sqlite3
db.sqlite3-journal
backend/staticfiles/

# Media — uploaded attendance photos and face enrolment images.
# These are biometric data belonging to real students. They must never
# reach a git repository, public or private.
backend/media/

# Face models (downloaded from the class server, not versioned)
backend/models/*.onnx

# Node / frontend
node_modules/
frontend/dist/
*.tsbuildinfo
npm-debug.log*
.env.local
.env.*.local

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
